<Page x:Class="ToyStoreByVlasovAndry.Content.UserPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ToyStoreByVlasovAndry.Content"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1000"
      Title="UserPage">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="400"/>
        </Grid.RowDefinitions>
        
        <StackPanel>
            <Label x:Name="username" Width="200" Height="30"/>
            <Label Content="Последний вход: " Width="200" Height="30"/>
        </StackPanel>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="240"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#E0FFFF" BorderBrush="Black" BorderThickness="1" CornerRadius="15" MinHeight="300" MaxHeight="300" MinWidth="220" MaxWidth="220" Margin="5" VerticalAlignment="Top">
                <StackPanel Orientation="Vertical">
                    <Label Content="Функции" HorizontalAlignment="Center"/>
                    <Button x:Name="addButton" Content="Добавить" Margin="5" Width="200" Height="30" Click="addButton_Click" FontWeight="Bold" Background="#E0FFFF"/>
                    <Button Content="Выйти из аккаунта" Margin="5" Width="200" Height="30" Click="goBack_Click" FontWeight="Bold" Background="#E0FFFF"/>
                    <Button Content="Выйти из приложения" Margin="5" Width="200" Height="30" Click="BtExit_Click" FontWeight="Bold" Background="#E0FFFF"/>
                    <Button x:Name="BtDelAcc" Content="Удалить аккаунт" Margin="5" Width="200" Height="30" Click="BtDelAcc_Click" FontWeight="Bold" Foreground="#FFD02626" Background="#E0FFFF"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#E0FFFF" BorderBrush="Black" BorderThickness="1" CornerRadius="15" MinHeight="300" MaxHeight="300" MinWidth="220" MaxWidth="220" Margin="5" VerticalAlignment="Top">
                <StackPanel Orientation="Vertical" Grid.Row="1" HorizontalAlignment="Center">
                    <Label Content="Меню перемешения" HorizontalAlignment="Center"/>
                    <Button x:Name="CartButton" Content="Корзина" Margin="5" Width="200" Height="30" Click="CartButton_Click" FontWeight="Bold" Background="#E0FFFF"/>
                    <Button Content="Магазин" Margin="5" Width="200" Height="30" Click="Button_Click" FontWeight="Bold" Background="#E0FFFF"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
